---
layout: layouts/base.njk
title: Tags
description: A list of all blog tags.
fontPreload: true
---

<div class="flex flex-col h-dvh justify-between">
  {% include "components/navigation.njk" %}
  <div class="max-w-7xl mx-auto mb-auto w-full px-5 mt-8">
    <h1 class="text-xl sm:text-2xl md:text-3xl font-bold text-white mb-5">{{ title }}</h1>
    <ul class="list-disc marker:text-white ml-10">
      {% for tag in collections.getTags %}
        {% if tag != 'posts' %}
          <li>
            <a href="/tags/{{ tag | slugify }}/">
              <span class="text-white hover:text-blue-300 hover:underline">#{{ tag }}</span>
            </a>
          </li>
        {% endif %}
      {% endfor %}
    </ul>
  </div>
  {% include "components/footer.njk" %}
</div>
